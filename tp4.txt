Buenas profesor, pude resolver los ejercicios. En el punto 4 no pude generar nuevos array en base a los que tenia, pero
resolvi las operaciones imprimiendo la solucion en base al array que tenia. En comentarios /**/ deje el intento de crear
nuevos arrays.
En el ejercicio 7 me salio, solo una cosita cuando muestro la recaudacion minima, no logre mostrar la posicion correspondiente
al minimo, sino que me repite la del maximo. No le encontre la vuelta. Saludos!



--------------------------------------------------------------------------------------
EJERCICIO 4 INCISO A

#include <stdio.h>
#include <stdlib.h>

void borrarElemento(int arregloB[], int k);
int main()
{
   /*Dado un arreglo A, generar un nuevo arreglo B, eliminando del arreglo A los
elementos que sean iguales a K.(K valor ingresado previamente).*/

    int i, n, k, cuenta=0, bn=0;
    printf("\n\nCuantos lugares quiere en el vector? ");
    scanf("%d", &n);

    int arregloA[n];


    for(i = 0; i < n; i++){
        printf("Que valor quiere en la posicion %d\n", i);
        scanf("%d", &arregloA[i]);}

    printf("\n\nIngrese el elemento que quiere eliminar : \n ");
    scanf("%d", &k);


    printf("El arreglo luego de eliminar el elemento ingresado es: ");

    for (i=0; i<n; i++)
    {
        if(arregloA[i]!=k)
        printf("%3d",arregloA[i]);
    }

    printf("\n\n");
/*
    for (i=0; i<n; i++)
        if( k==arregloA[i])
        cuenta++;
            if (cuenta==0)
            printf("\nEl arregloB es igual al arregloA, no hubo modificaciones \n");
            else
            printf("\nSe eliminaran %d elementos del arregloA \n",cuenta);


    {
    bn= n-cuenta;
    int arregloB[bn];

        for(i=0; i<bn;i++)
        {
            if (k==arregloA[i])
            { arregloB[bn]=((arregloA[i]-k));
            }
            printf("\nEl arregloB es: \n");

    for(i=0; i <n; i=i+1)
    printf("%3d", arregloB[bn]);
    getch();


        }
    }*/

    return 0;
}




--------------------------------------------------------------------------------------
EJERCICIO 4 INCISO B

#include <stdio.h>
#include <stdlib.h>

void main()
{
    /*Dado un arreglo A de N componentes float, crear dos nuevos vectores B y C tales
que: B, tenga solo las componentes de A que ocupan lugar par y C solo las
componentes de A que ocupan lugar impar. Imprimir los tres vectores.*/

    int n,i, par=0, impar=0;


    printf("Cuantos elementos quieres en tu array? \n");
    fflush(stdin);
    scanf("%d",&n);

    int A[n];

    for (i=0; i<n; i++){
        printf("Que valor quiere en la posicion %d\n", i);
        fflush(stdin);
        scanf("%d", &A[i]);

        }

    printf("\nEl arreglo A es: \n");

    for(i=0; i <n; i=i+1)
    printf("%3d", A[i]);
    getch();


    printf("\n\nLos elementos en posiciones pares son los siguientes: \n");
    for (i=0; i<n; i+=2)
    {
        printf("\n%d", A[i]);
    }


     printf("\n\nLos elementos en posiciones impares son los siguientes: \n");
    for (i=1; i<n; i+=2)
    {
        printf("\n%d\n\n", A[i]);
    }

   /* for (i=1; i<=n; i++){
        if (i%2==0)
        {
            par=par+1;
        }
        else
        {
            impar=impar+1;
        }
    }
    printf("\n\n hay %d elementos en posiciones pares ",par);
    printf("\n\n hay %d elementos en posiciones impares ",impar);

    int B[par];
    int C[impar];

    for (i=1; i<=n; i++)
    {
        if (i%2==0)
        {
            B[par]= A[i%2==0];
        }
        else
        {
            C[impar]= A[i]-B[par];
        }

    }

    printf("\n\nEl arreglo B es: \n");

    for(i=1; i <=n; i=i+1)
    printf("%3d", B[par]);
    getch();

    printf("\n\nEl arreglo C es: \n");

    for(i=1; i <=n; i=i+1)
    printf("%3d", C[impar]);
    getch();
*/

    return 0;
}



-------------------------------------------------------------------------------------------------------
EJERCICIO 4 INCISO C


#include <stdio.h>
#include <stdlib.h>
#include<conio.h>

int main()
{
    /*Calcular la suma de los valores negativos y el producto de los valores positivos
distintos de cero de un vector A de N componentes enteras.*/
    int i,j,suma=0,n,prod=1;

    printf("\nCuantos elementos queres en el array? \n");
    fflush(stdin);
    scanf("%d",&n);

    int A[n];

    /* Capturando los valores del arreglo */

    printf("\n\nIngrese valores positivos y negativos, que no sean ceros, ambos enteros\n\n");
    for (i=0; i <n; i=i+1)
    {
    printf("Ingrese valor de A[%d]:", i+1);
    scanf("%d",&A[i]);
    }

    /* Mostrando el contenido del archivo */
    for(i=0; i <n; i=i+1)
    printf("%3d", A[i]);
    getch();

    for(i=0; i <n; i=i+1)
        if(A[i]<0)
        {
            suma=suma+A[i];
        }
        else
        {
            prod=A[i]*prod;
        }
    printf("\n\nla suma es %d",suma);
    printf("\n\nel producto es %d\n\n",prod);

    return 0;
}




-----------------------------------------------------------------------------------------------------
EJERCICIO 7


#include <stdio.h>
#include <stdlib.h>
#define n 7

int main()
{
    /*En un comercio se registran las ventas por cada uno de los siete días de la semana,
de la siguiente forma: en CAJA1 se indica el dinero que ingreso en efectivo y en
CAJA2 el monto facturado en tarjetas de crédito.
Al final de la semana se desea determinar:
• El total de las ventas
• Cual fue el día que ingreso menos efectivo
• El día que más se facturo, porcentaje facturado en efectivo y porcentaje
facturado en tarjetas de crédito.*/

    char dias [n][10];
    float caja1[n];
    float caja2[n];
    float vector3[n];
    int i, pos;
    float totalcaja1=0, totalcaja2=0, mayor, menor, porcefvo, porctarj;

    for(i=0; i <n; i=i+1)
    {
        printf("\nIngrese el dia: \n");
        fflush(stdin);
        scanf("%s",&dias[i]);
        printf("Ingrese la recaudacion en efectivo:");
        fflush(stdin);
        scanf("%g",&caja1[i]);
        printf("Ingrese lo cobrado con tarjeta:");
        fflush(stdin);
        scanf("%g",&caja2[i]);

    }

    printf("\n\nLos dias ingresados son: \n\n");

    for (i=0; i<n; i++)

        printf("%s\t",dias[i]);
        getch();

    printf("\nLa recaudacion en efectivo es: \n\n");

    for (i=0; i<n; i++){
        printf("%g\t",caja1[i]);
        }

    printf("\nLa recaudacion con tarjeta es: \n\n");
    for (i=0; i<n; i++){
        printf("%g\t",caja2[i]);
        }

    for (i=0; i<n; i++){
    totalcaja1+= caja1[i];}
    printf("\n\nEl total de las ventas en efectivo es: %g  pesos.-",totalcaja1);

    for (i=0; i<n; i++){
    totalcaja2+= caja2[i];}
    printf("\n\nEl total de las ventas con tarjeta es: %g  pesos.-",totalcaja2);

    printf("\n\nEl total de las ventas de la semana es: %g  pesos.-",totalcaja1+totalcaja2);

    printf("\n\nRecaudacion por dia: \n\n");
    for(i=0;i<n;i++){

    vector3[i]=caja1[i]+caja2[i];
    printf("%3g\t",vector3[i]);
    }

    mayor=vector3[0], menor=vector3[0];

    for (i=0; i<n; i++){
    if (vector3[i]> mayor){
    pos=i;
    mayor=vector3[i];}
    /*}

    for (i=0; i<n; i++) {*/
    if (vector3[i]< menor){

    menor=vector3[i];}
        }

    printf("\n\nLa mayor recaudacion corresponde a la posision %d del array con un total de %g pesos.-\n",pos, mayor);
    printf("\nLa menor recaudacion corresponde a la posicion %d del array por un monto de %g pesos.-\n",pos, menor);

    porcefvo= totalcaja1/(totalcaja1+totalcaja2);
    printf("\nEl porcentaje cobrado en efectivo fue : %g porciento del total de la recaudacion\n",porcefvo*100);

    porctarj= totalcaja2/(totalcaja1+totalcaja2);
    printf("\nEl porcentaje cobrado en tarjeta fue : %g porciento del total de la recaudacion\n",porctarj*100);

    return 0;
}




----------------------------------------------------------------------------------------------------------------------